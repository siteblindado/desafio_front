const types = {
  //Define Types
};

const INITIAL_STATE = {};

export const dataReducer = (state = INITIAL_STATE, action: any) => {

  switch (action.type) {
    // case  ...: 
    default:
      return state;
  }
};
